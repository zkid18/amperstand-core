---
title: Introduction
description: "Self-hosted personal vault. Capture URLs, YouTube, RSS, and newsletters from any client into one searchable store."
---

## What it is

Amperstand is a **self-hosted personal vault**. It has two parts you need to understand:

1. **The server (`amperstand-core`)** — the only thing that touches your data. Runs on a box you control: a $4 droplet, a home NAS, even localhost. Exposes a REST API.
2. **Clients (anything that speaks HTTP to the server)** — the official CLI (`amperstand`), a Telegram bot, a Chrome extension, your own agent code. Capture surfaces, query surfaces, integration glue. The bot and extension aren't special — they're just two HTTP clients we ship as reference implementations. You can replace either of them, or add a fifth client tomorrow, and the server doesn't care.

That's the entire architecture. One server. N clients. HTTP between them.

## Architecture

```
   ┌───────────┐   ┌──────────────┐   ┌────────────┐   ┌─────────────┐
   │    CLI    │   │ Telegram bot │   │ Chrome ext │   │  anything   │
   └─────┬─────┘   └──────┬───────┘   └──────┬─────┘   └──────┬──────┘
         │                │                  │                │
         └────────────────┴────────┬─────────┴────────────────┘
                                   │  HTTP + API key
                           ┌───────▼──────────┐
                           │  amperstand-core │
                           └──────────────────┘
```

**Clients are interchangeable.** Today we ship a CLI, a Telegram bot, and a Chrome extension. Build a Discord bot, an iOS Shortcut, an n8n flow — the server doesn't care which client made the request.

## What it captures

| Surface | Extractor |
| --- | --- |
| Articles & blog posts | trafilatura; Anysite fetch and browser render for pages that block server IPs |
| YouTube videos | Caption track via Anysite, title and channel via oembed. No captions → metadata stub. |
| LinkedIn posts | Full post text via Anysite; post videos transcribed with Whisper when enabled |
| Reddit posts | Post record via Anysite |
| Twitter / X | `fxtwitter` rewrite + thread reconstruction |
| RSS / Atom feeds | `feedparser` + per-item extraction |
| Newsletter emails | `.eml` parsing, IMAP fetch loop |

All extraction runs **on the server**. Clients just hand over a URL (or HTML, or an .eml file) and wait for the result.

## Why this shape

- **One vault, many clients.** Capture from your phone via Telegram, one-click from the browser, batch from a script — everything lands in the same searchable store.
- **Data sovereignty.** The vault is plain markdown files on a server you own. The tool can disappear; the files don't.
- **Stable surface.** The HTTP API is the contract. Build any client in any language. Your own agents query the same endpoints the bot does.

## Set up your server

<CardGroup cols={2}>
  <Card title="Bare-metal install (one page)" icon="rocket" href="/quickstart">
    Clone, run `bootstrap.sh`, capture a URL. One canonical path with an expandable DigitalOcean walkthrough for first-timers. ~10 min if you have a host, ~30 if you're starting from scratch.
  </Card>
  <Card title="Docker Compose" icon="docker" href="/server-install-docker">
    `docker compose up -d`. Homelab-friendly; drops into your existing compose stack.
  </Card>
</CardGroup>

Once your server is up:

<CardGroup cols={2}>
  <Card title="Install the CLI" icon="terminal" href="/cli-install">
    `pip install amperstand`, point it at your server.
  </Card>
  <Card title="Use the CLI (start here)" icon="play" href="/cli-quickstart">
    The six things to know how to do. Capture, search, subscribe to a feed, read a doc back.
  </Card>
  <Card title="Plug it into an AI agent" icon="robot" href="/agents-overview">
    Make your vault the corpus your agent queries. Skills, system prompts, shell-out — three patterns.
  </Card>
  <Card title="Build a custom client" icon="code" href="/examples/build-your-own">
    The HTTP API is the contract. Any language, any tool.
  </Card>
</CardGroup>
